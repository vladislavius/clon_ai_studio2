# 💻 Локальная разработка vs GitHub vs Vercel

## 🔍 Понимание разницы

### 1. **Локальная разработка** (на вашем компьютере)
- Вы клонируете проект с GitHub на свой компьютер
- Запускаете `npm run dev` локально
- Файл `.env.local` нужен только здесь
- Изменения видны только вам

### 2. **GitHub** (хранилище кода)
- Это место, где хранится код
- Вы отправляете изменения через `git push`
- Другие могут скачать ваш код

### 3. **Vercel** (хостинг для продакшена)
- Это место, где работает ваш сайт для пользователей
- Vercel автоматически деплоит код с GitHub
- Нужно настроить переменные окружения в интерфейсе Vercel

---

## 📋 Шаг 1: Работа локально (на вашем компьютере)

### Если проект уже на вашем компьютере:

1. **Откройте терминал**
2. **Перейдите в папку проекта:**
   ```bash
   cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2
   ```
3. **Проверьте, что файл .env.local существует:**
   ```bash
   ls -la .env.local
   ```
4. **Запустите локальный сервер:**
   ```bash
   npm run dev
   ```
5. **Откройте браузер:**
   - Обычно это `http://localhost:5173` (Vite покажет точный адрес)
   - Изменения видны только вам

### Если проекта нет на компьютере (нужно клонировать):

```bash
# 1. Перейдите в нужную папку
cd ~/Desktop

# 2. Клонируйте репозиторий
git clone https://github.com/vladislavius/clon_ai_studio2.git

# 3. Перейдите в проект
cd clon_ai_studio2

# 4. Установите зависимости
npm install

# 5. Создайте файл .env.local (см. ниже)
# 6. Запустите сервер
npm run dev
```

---

## 🔧 Шаг 2: Настройка .env.local для локальной разработки

Файл `.env.local` нужен **только для локальной разработки** на вашем компьютере.

### Создайте файл `.env.local` в корне проекта:

```bash
# В терминале, в папке проекта:
nano .env.local
# или
open -a TextEdit .env.local
```

### Добавьте содержимое:

```env
VITE_SUPABASE_URL=https://supabase.assisthelp.ru
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlzcyI6InN1cGFiYXNlIiwiaWF0IjoxNzY1NjU5NjAwLCJleHAiOjE5MjM0MjYwMDB9.-DxQZP7k4N9kt6Xfe7gFjTVtiLZ7BIMJQz44PWFkz-k
```

### Сохраните и перезапустите сервер:

```bash
# Остановите сервер (Ctrl+C)
# Запустите снова
npm run dev
```

---

## 🌐 Шаг 3: Настройка Vercel (для продакшена)

На Vercel нужно настроить переменные окружения через их интерфейс.

### 1. Откройте Vercel Dashboard

Перейдите на: https://vercel.com/dashboard

### 2. Найдите ваш проект

Выберите проект `clon_ai_studio2` (или как он называется)

### 3. Перейдите в Settings → Environment Variables

1. Нажмите на проект
2. Перейдите в **Settings**
3. Выберите **Environment Variables**

### 4. Добавьте переменные окружения

Нажмите **"Add New"** и добавьте:

**Переменная 1:**
- **Name:** `VITE_SUPABASE_URL`
- **Value:** `https://supabase.assisthelp.ru`
- **Environment:** Выберите `Production`, `Preview`, `Development` (или все)

**Переменная 2:**
- **Name:** `VITE_SUPABASE_ANON_KEY`
- **Value:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlzcyI6InN1cGFiYXNlIiwiaWF0IjoxNzY1NjU5NjAwLCJleHAiOjE5MjM0MjYwMDB9.-DxQZP7k4N9kt6Xfe7gFjTVtiLZ7BIMJQz44PWFkz-k`
- **Environment:** Выберите `Production`, `Preview`, `Development` (или все)

### 5. Сохраните и передеплойте

После добавления переменных:
1. Нажмите **"Save"**
2. Перейдите в **Deployments**
3. Нажмите на последний деплой → **"Redeploy"**
4. Или просто сделайте новый коммит и push - Vercel автоматически передеплоит

---

## 🔄 Полный цикл разработки

### 1. Локальная разработка:

```bash
# На вашем компьютере
cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2
npm run dev
# Откройте http://localhost:5173
```

### 2. Сохранение изменений:

```bash
# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push origin main
```

### 3. Автоматический деплой на Vercel:

- Vercel автоматически обнаружит изменения на GitHub
- Автоматически передеплоит сайт
- Использует переменные окружения, которые вы настроили в Vercel Dashboard

---

## ⚠️ Важные моменты

### Файл .env.local:
- ✅ Нужен для локальной разработки
- ❌ НЕ нужен на GitHub (уже в .gitignore)
- ❌ НЕ нужен на Vercel (используйте Environment Variables в интерфейсе)

### Переменные окружения:
- **Локально:** в файле `.env.local`
- **На Vercel:** в Settings → Environment Variables
- **На GitHub:** НЕ храните! (они в .gitignore)

### Когда что использовать:

| Действие | Где делать |
|----------|------------|
| Разработка и тестирование | Локально (`npm run dev`) |
| Хранение кода | GitHub (`git push`) |
| Публичный сайт для пользователей | Vercel (автоматически) |

---

## 🎯 Быстрая справка

### Локальная разработка:
```bash
cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2
npm run dev
```

### Отправка на GitHub:
```bash
git add .
git commit -m "Изменения"
git push origin main
```

### Настройка Vercel:
1. Vercel Dashboard → Ваш проект → Settings → Environment Variables
2. Добавьте `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`
3. Redeploy проект

---

## ❓ FAQ

**Q: Мне нужно запускать `npm run dev` на Vercel?**
A: Нет! Vercel автоматически собирает и деплоит проект. Вы запускаете `npm run dev` только локально на своем компьютере.

**Q: Где создать .env.local?**
A: Только локально, в корне проекта на вашем компьютере. На Vercel используйте Environment Variables в интерфейсе.

**Q: Как обновить сайт на Vercel?**
A: Просто сделайте `git push` - Vercel автоматически передеплоит.

**Q: Почему на Vercel ошибка подключения?**
A: Проверьте, что вы добавили переменные окружения в Vercel Dashboard → Settings → Environment Variables.






