# Руководство по деплою на GitHub Pages

## ✅ Что уже сделано

1. ✅ Создан GitHub Actions workflow (`.github/workflows/deploy.yml`)
2. ✅ Настроен `vite.config.ts` для поддержки GitHub Pages
3. ✅ Все изменения закоммичены

## 📋 Следующие шаги

### 1. Настройка GitHub Pages

1. Перейдите в настройки репозитория: `Settings` → `Pages`
2. В разделе `Source` выберите:
   - **Source**: `GitHub Actions`
3. Сохраните изменения

### 2. Настройка Secrets (переменные окружения)

Для работы приложения нужны переменные окружения Supabase:

1. Перейдите в `Settings` → `Secrets and variables` → `Actions`
2. Добавьте следующие secrets:
   - `VITE_SUPABASE_URL` - URL вашего Supabase проекта
   - `VITE_SUPABASE_ANON_KEY` - Anon key из Supabase

### 3. Запушить изменения

Если push не прошел автоматически, выполните вручную:

```bash
git push origin perf-auth-virtualize
```

Или если хотите задеплоить из ветки `main`:

```bash
git checkout main
git merge perf-auth-virtualize
git push origin main
```

### 4. Проверка деплоя

1. Перейдите в `Actions` в вашем репозитории
2. Дождитесь завершения workflow `Deploy to GitHub Pages`
3. После успешного деплоя приложение будет доступно по адресу:
   ```
   https://vladislavius.github.io/clon_ai_studio2/
   ```

## 🔧 Альтернатива: Vercel

Если хотите использовать Vercel вместо GitHub Pages:

1. Зайдите на [vercel.com](https://vercel.com)
2. Подключите ваш GitHub репозиторий
3. Добавьте переменные окружения:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
4. Vercel автоматически задеплоит приложение

## 📝 Примечания

- GitHub Pages бесплатен для публичных репозиториев
- Деплой происходит автоматически при каждом push в ветки: `main`, `master`, или `perf-auth-virtualize`
- Если нужно изменить base path, отредактируйте `vite.config.ts` (строка 11)

