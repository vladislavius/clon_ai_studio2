# 🔒 Руководство по применению критических исправлений безопасности

**Дата:** Декабрь 2024  
**Статус:** В процессе реализации

---

## 📋 Чеклист выполнения

### ✅ 1. Защита от XSS атак

**Статус:** ✅ Код готов, требуется установка пакета

**Шаги:**

1. **Установить DOMPurify:**
```bash
npm install dompurify @types/dompurify
```

2. **Файлы созданы:**
- ✅ `utils/sanitize.ts` - утилиты санитизации
- ⚠️ Требуется обновить компоненты для использования санитизации

3. **Компоненты для обновления:**
- `components/EmployeeModal.tsx` - поля описаний
- `components/EmployeeList.tsx` - отображение данных
- `components/OrgChart.tsx` - описания департаментов
- `components/StatisticsTab.tsx` - названия статистик

**Пример использования:**
```typescript
import { sanitizeText } from '../utils/sanitize';

// В компоненте:
<div>{sanitizeText(userContent)}</div>
```

---

### ✅ 2. Rate limiting для аутентификации

**Статус:** ✅ Реализовано

**Файлы:**
- ✅ `hooks/useRateLimit.ts` - хук для rate limiting
- ✅ `components/Auth.tsx` - интегрирован rate limiting

**Функции:**
- Максимум 5 попыток входа
- Блокировка на 15 минут после 5 неудачных попыток
- Экспоненциальная задержка между попытками
- Визуальный индикатор оставшихся попыток

**Дополнительно:**
- Настроить rate limiting в Supabase Dashboard:
  - Authentication → Settings → Enable email rate limiting

---

### ⚠️ 3. Безопасное хранение токенов

**Статус:** ⚠️ SQL скрипт готов, требуется миграция компонентов

**Шаги:**

1. **Выполнить SQL скрипт:**
```sql
-- Выполнить в Supabase SQL Editor
-- Файл: create_integration_tokens_table.sql
```

2. **Обновить компоненты:**
- `components/IntegrationsPanel.tsx` - использовать новую таблицу вместо localStorage
- Создать утилиту `utils/integrations.ts` для работы с токенами

**Пример миграции:**
```typescript
// Было:
localStorage.setItem('telegram_bot_token', token);

// Стало:
await saveIntegrationToken('telegram', token);
```

**Файлы:**
- ✅ `create_integration_tokens_table.sql` - SQL скрипт для создания таблицы
- ⚠️ Требуется создать `utils/integrations.ts`
- ⚠️ Требуется обновить `components/IntegrationsPanel.tsx`

---

### ✅ 4. Проверка RLS политик

**Статус:** ✅ Скрипт проверки готов

**Шаги:**

1. **Выполнить скрипт проверки:**
```sql
-- Выполнить в Supabase SQL Editor
-- Файл: security_check.sql
```

2. **Проверить результаты:**
- Все таблицы должны иметь RLS включенный
- Все таблицы должны иметь политики безопасности
- Функция `is_admin()` должна существовать
- Не должно быть политик для анонимных пользователей

3. **Протестировать доступ:**
- Попробовать выполнить запросы без аутентификации
- Попробовать выполнить запросы как обычный пользователь (не админ)
- Убедиться, что модификация данных доступна только админам

**Файлы:**
- ✅ `security_check.sql` - скрипт проверки безопасности

---

## 🚀 Порядок применения исправлений

### Этап 1: Критически важно (Сегодня)

1. ✅ **Rate limiting** - Уже реализовано, работает
2. ⚠️ **XSS защита** - Установить пакет и обновить компоненты
3. ⚠️ **Проверка RLS** - Выполнить `security_check.sql`

### Этап 2: Важно (В течение недели)

4. ⚠️ **Безопасное хранение токенов** - Выполнить миграцию

---

## 📝 Инструкции по применению

### 1. Установка DOMPurify

```bash
cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2
npm install dompurify @types/dompurify
```

### 2. Выполнение SQL скриптов

1. Откройте Supabase Dashboard
2. Перейдите в SQL Editor
3. Выполните скрипты в следующем порядке:
   - `security_check.sql` - проверка текущего состояния
   - `create_integration_tokens_table.sql` - создание таблицы для токенов

### 3. Обновление компонентов для XSS защиты

После установки DOMPurify, обновить компоненты:
- Импортировать `sanitizeText` из `utils/sanitize`
- Применить к всем пользовательским данным перед отображением

---

## ✅ Проверка работоспособности

### Rate limiting

1. Попробуйте войти с неверным паролем 5 раз
2. Должна появиться блокировка на 15 минут
3. Должен отображаться индикатор оставшихся попыток

### RLS политики

1. Выполните `security_check.sql`
2. Все проверки должны пройти успешно
3. Попробуйте выполнить запросы без аутентификации - должны быть отклонены

### XSS защита

1. Попробуйте ввести HTML/JavaScript в поля ввода
2. При отображении код должен быть экранирован
3. Проверьте консоль браузера - не должно быть выполненных скриптов

---

## 🔗 Связанные файлы

- `SECURITY_AUDIT_SUMMARY.md` - Краткая сводка аудита
- `SECURITY_AND_FUNCTIONALITY_AUDIT.md` - Полный отчет аудита
- `utils/sanitize.ts` - Утилиты санитизации
- `hooks/useRateLimit.ts` - Хук rate limiting
- `components/Auth.tsx` - Компонент аутентификации с rate limiting
- `security_check.sql` - Скрипт проверки RLS
- `create_integration_tokens_table.sql` - Скрипт создания таблицы токенов

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера на ошибки
2. Проверьте логи Supabase
3. Убедитесь, что все SQL скрипты выполнены успешно
4. Проверьте, что пакеты установлены (`npm list dompurify`)




