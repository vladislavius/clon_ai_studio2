# 🔐 Настройка переменных окружения

## Важно: Создайте файл .env.local

После исправления критических проблем безопасности, API ключи Supabase теперь должны храниться в переменных окружения.

### Шаги настройки:

1. **Создайте файл `.env.local` в корне проекта**

2. **Добавьте следующие переменные:**

```env
VITE_SUPABASE_URL=https://supabase.assisthelp.ru
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlzcyI6InN1cGFiYXNlIiwiaWF0IjoxNzY1NjU5NjAwLCJleHAiOjE5MjM0MjYwMDB9.-DxQZP7k4N9kt6Xfe7gFjTVtiLZ7BIMJQz44PWFkz-k
```

**⚠️ ВАЖНО:** 
- Файл `.env.local` уже добавлен в `.gitignore` и не будет попадать в git
- Используйте ваши реальные ключи из Supabase Project Settings -> API
- Никогда не коммитьте файл `.env.local` в репозиторий

### Как получить ключи:

1. Зайдите в ваш проект Supabase
2. Перейдите в **Project Settings** -> **API**
3. Скопируйте:
   - **Project URL** → `VITE_SUPABASE_URL`
   - **anon public key** → `VITE_SUPABASE_ANON_KEY`

### После создания файла:

1. Перезапустите dev сервер (`npm run dev`)
2. Приложение автоматически подхватит переменные окружения

---

## ✅ Исправленные проблемы:

1. ✅ API ключи перемещены в переменные окружения
2. ✅ Исправлена проверка окружения (`process.env` → `import.meta.env`)
3. ✅ Исправлен смешанный язык в ErrorBoundary
4. ✅ Обновлен `.gitignore` для защиты `.env.local`











