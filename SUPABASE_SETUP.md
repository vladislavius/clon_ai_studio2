# 🗄️ Инструкция по настройке Supabase для HR System Pro

## 📋 Содержание

1. [Применение схемы базы данных](#1-применение-схемы-базы-данных)
2. [Настройка ролей пользователей](#2-настройка-ролей-пользователей)
3. [Инициализация начальных данных](#3-инициализация-начальных-данных)
4. [Проверка безопасности](#4-проверка-безопасности)
5. [Миграция существующих данных](#5-миграция-существующих-данных)

---

## 1. Применение схемы базы данных

### Шаг 1: Откройте SQL Editor в Supabase

1. Зайдите в ваш проект Supabase
2. Перейдите в раздел **SQL Editor**
3. Создайте новый запрос

### Шаг 2: Примените основную схему

Скопируйте содержимое файла `supabase_schema_complete.sql` и выполните его в SQL Editor.

**Важно:** Этот скрипт создаст:
- ✅ Все необходимые таблицы
- ✅ RLS политики безопасности
- ✅ Функции для работы с ролями
- ✅ Триггеры для автоматического обновления `updated_at`
- ✅ Storage buckets для файлов
- ✅ Индексы для производительности

### Шаг 3: Проверьте созданные таблицы

После выполнения скрипта проверьте, что созданы следующие таблицы:

- `employees` - сотрудники
- `employee_attachments` - вложения сотрудников
- `statistics_definitions` - определения статистик
- `statistics_values` - значения статистик
- `org_metadata` - метаданные оргструктуры
- `departments` - департаменты
- `subdepartments` - поддепартаменты

---

## 2. Настройка ролей пользователей

### Вариант A: Через Supabase Dashboard (рекомендуется)

1. Перейдите в **Authentication** → **Users**
2. Найдите пользователя, которому нужно дать права администратора
3. Нажмите на пользователя → **Edit**
4. В разделе **User Metadata** или **App Metadata** добавьте:

**Для App Metadata (рекомендуется):**
```json
{
  "role": "admin"
}
```

**Или для User Metadata:**
```json
{
  "is_admin": true
}
```

### Вариант B: Через SQL (для массовой настройки)

```sql
-- Установить роль администратора для конкретного email
UPDATE auth.users
SET app_metadata = jsonb_set(
  COALESCE(app_metadata, '{}'::jsonb),
  '{role}',
  '"admin"'
)
WHERE email = 'admin@example.com';

-- Или через user_metadata
UPDATE auth.users
SET user_metadata = jsonb_set(
  COALESCE(user_metadata, '{}'::jsonb),
  '{is_admin}',
  'true'
)
WHERE email = 'admin@example.com';
```

### Проверка роли администратора

Выполните запрос для проверки:

```sql
SELECT 
  email,
  app_metadata->>'role' as role,
  user_metadata->>'is_admin' as is_admin
FROM auth.users
WHERE email = 'your-email@example.com';
```

---

## 3. Инициализация начальных данных

### Шаг 1: Примените скрипт инициализации

Выполните содержимое файла `supabase_init_data.sql` в SQL Editor.

Этот скрипт создаст:
- ✅ Все департаменты из `constants.ts`
- ✅ Все поддепартаменты
- ✅ Метаданные компании (owner)

### Шаг 2: Проверьте данные

```sql
-- Проверить департаменты
SELECT * FROM public.departments ORDER BY sort_order;

-- Проверить поддепартаменты
SELECT * FROM public.subdepartments ORDER BY department_id, sort_order;

-- Проверить метаданные компании
SELECT * FROM public.org_metadata WHERE type = 'company';
```

---

## 4. Проверка безопасности

### Тест 1: Проверка RLS политик

```sql
-- Войдите как обычный пользователь (не админ)
-- Попробуйте выполнить:

-- Это должно работать (чтение разрешено всем)
SELECT * FROM public.employees LIMIT 1;

-- Это должно НЕ работать (только админы могут изменять)
INSERT INTO public.employees (full_name, position) 
VALUES ('Test', 'Test');
-- Должна быть ошибка: "new row violates row-level security policy"
```

### Тест 2: Проверка функции is_admin()

```sql
-- Выполните как админ
SELECT public.is_admin();
-- Должно вернуть: true

-- Выполните как обычный пользователь
SELECT public.is_admin();
-- Должно вернуть: false
```

### Тест 3: Проверка Storage политик

1. Перейдите в **Storage** → **Policies**
2. Убедитесь, что созданы политики для:
   - `employee-photos`
   - `employee-documents`
3. Проверьте, что политики требуют `is_admin()` для записи/удаления

---

## 5. Миграция существующих данных

Если у вас уже есть данные в старой схеме, выполните миграцию:

### Шаг 1: Экспорт существующих данных

```sql
-- Экспорт сотрудников
COPY (SELECT * FROM public.employees) TO '/tmp/employees_backup.csv' CSV HEADER;

-- Экспорт статистик
COPY (SELECT * FROM public.statistics_definitions) TO '/tmp/stats_def_backup.csv' CSV HEADER;
COPY (SELECT * FROM public.statistics_values) TO '/tmp/stats_vals_backup.csv' CSV HEADER;
```

### Шаг 2: Применение новой схемы

Выполните `supabase_schema_complete.sql` (он использует `IF NOT EXISTS`, поэтому безопасен)

### Шаг 3: Импорт данных

```sql
-- Импорт сотрудников (адаптируйте под вашу структуру)
-- Внимание: проверьте соответствие колонок!
```

---

## 🔒 Безопасность: Важные моменты

### ✅ Что защищено RLS:

1. **Все таблицы** имеют RLS включенный
2. **Чтение** разрешено всем авторизованным пользователям
3. **Запись/Изменение/Удаление** разрешено только администраторам
4. **Storage buckets** защищены политиками

### ⚠️ Что нужно проверить:

1. ✅ Убедитесь, что все администраторы имеют роль в `app_metadata` или `user_metadata`
2. ✅ Удалите старые политики, если они были созданы ранее
3. ✅ Проверьте, что `ANON_KEY` не используется для критических операций
4. ✅ Регулярно проверяйте логи доступа в Supabase Dashboard

---

## 6. Настройка Storage для файлов

### Проверка Storage buckets

После применения схемы проверьте, что созданы buckets:

1. Перейдите в **Storage** → **Buckets**
2. Убедитесь, что созданы:
   - ✅ `employee-files` (для фотографий, лимит 5MB)
   - ✅ `employee-docs` (для документов, лимит 10MB)

### Тестирование загрузки файлов

1. **Загрузите тестовое изображение:**
   - Откройте карточку сотрудника
   - Попробуйте загрузить фотографию
   - Проверьте, что файл появился в bucket `employee-files`

2. **Загрузите тестовый документ:**
   - В разделе "Файлы" загрузите документ
   - Проверьте, что файл появился в bucket `employee-docs`
   - Проверьте, что запись создалась в таблице `employee_attachments`

3. **Проверьте удаление:**
   - Удалите attachment из карточки сотрудника
   - Проверьте, что файл автоматически удалился из storage (триггер)

### Очистка orphaned файлов

Если нужно очистить файлы без записей в БД:

```sql
SELECT * FROM public.cleanup_orphaned_files();
```

Эта функция:
- Найдет все файлы в storage без соответствующих записей в БД
- Удалит их
- Вернет количество удаленных файлов и ошибки (если есть)

## 🧪 Тестирование после настройки

### Проверочный список:

- [ ] Все таблицы созданы
- [ ] RLS политики работают (обычный пользователь не может изменять данные)
- [ ] Администратор может изменять данные
- [ ] Storage buckets созданы и защищены (`employee-files`, `employee-docs`)
- [ ] Функция `is_admin()` работает корректно
- [ ] Триггеры обновляют `updated_at` автоматически
- [ ] Триггер удаления файлов работает (при удалении attachment файл удаляется из storage)
- [ ] Начальные данные (департаменты) загружены
- [ ] Загрузка файлов работает корректно
- [ ] Валидация размера файлов работает

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи в Supabase Dashboard → **Logs**
2. Проверьте ошибки в SQL Editor
3. Убедитесь, что все расширения установлены (`uuid-ossp`, `pgcrypto`)

---

## 🎯 Следующие шаги

После настройки базы данных:

1. Обновите код фронтенда для использования новых таблиц `departments` и `subdepartments`
2. Убедитесь, что `useAuth` использует только роли из JWT (без fallback на email)
3. Протестируйте все CRUD операции
4. Настройте резервное копирование в Supabase

---

**Готово!** Ваша база данных настроена и защищена. 🎉

