# Исправление: Таблицы созданы, но приложение их не видит

## Проблема
Таблицы `onboarding_templates`, `onboarding_instances`, `onboarding_tasks` созданы в Supabase, но приложение возвращает ошибку 400 (Bad Request).

## Причина
**Кеш схемы PostgREST (Supabase API)** не обновлен после создания таблиц.

---

## ✅ Решение (пошагово)

### Шаг 1: Проверьте, что таблицы действительно созданы

Выполните в SQL Editor:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
  AND table_name IN ('onboarding_templates', 'onboarding_instances', 'onboarding_tasks');
```

**Ожидаемый результат:** 3 строки с названиями таблиц.

Если таблиц нет → выполните `create_onboarding_tables.sql` еще раз.

---

### Шаг 2: Обновите кеш схемы PostgREST

#### Вариант A: Через Supabase Dashboard (самый простой)

1. Откройте **Supabase Dashboard**: https://supabase.assisthelp.ru
2. Перейдите в **Settings** (настройки проекта)
3. Найдите раздел **API** или **Database**
4. Найдите кнопку **"Reload schema cache"** или **"Refresh schema"**
5. Нажмите на неё
6. Подождите 10-15 секунд

#### Вариант B: Через SQL (если нет кнопки в Dashboard)

Выполните в SQL Editor:

```sql
-- Обновление кеша схемы
NOTIFY pgrst, 'reload schema';
```

Или попробуйте:

```sql
SELECT pg_notify('pgrst', 'reload schema');
```

**Важно:** После выполнения подождите 10-15 секунд перед обновлением страницы приложения.

#### Вариант C: Перезапуск проекта (если есть доступ)

Если у вас есть доступ к настройкам проекта Supabase, можно перезапустить PostgREST сервис через Dashboard.

---

### Шаг 3: Проверьте RLS политики

Выполните скрипт `quick_check_onboarding.sql` для проверки:

- ✅ Таблицы существуют
- ✅ RLS включен
- ✅ Политики созданы

---

### Шаг 4: Обновите страницу приложения

1. Откройте DevTools (F12)
2. Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)
3. Обновите страницу (F5)

---

### Шаг 5: Проверьте результат

В консоли браузера:
- ❌ **Было:** `Failed to load resource: the server responded with a status of 400`
- ✅ **Должно быть:** Нет ошибок, данные загружаются

---

## 🔍 Диагностика

Если после обновления кеша проблема осталась:

### 1. Проверьте права доступа

Убедитесь, что вы авторизованы как администратор:

```sql
-- Проверка текущего пользователя
SELECT auth.email();

-- Проверка функции is_admin
SELECT public.is_admin();
```

### 2. Проверьте RLS политики

```sql
-- Список всех политик
SELECT tablename, policyname, cmd
FROM pg_policies
WHERE schemaname = 'public' 
  AND tablename IN ('onboarding_templates', 'onboarding_instances', 'onboarding_tasks');
```

Должно быть минимум:
- `onboarding_templates`: 2 политики (для админов и для чтения)
- `onboarding_instances`: 4 политики (для админов, чтение, создание, обновление)
- `onboarding_tasks`: 3 политики (для админов, чтение, обновление)

### 3. Временно отключите RLS для теста

⚠️ **Только для диагностики!**

```sql
-- Временно отключить RLS
ALTER TABLE onboarding_templates DISABLE ROW LEVEL SECURITY;
ALTER TABLE onboarding_instances DISABLE ROW LEVEL SECURITY;
ALTER TABLE onboarding_tasks DISABLE ROW LEVEL SECURITY;

-- Проверьте, работает ли доступ
-- Если работает - проблема в RLS политиках

-- ВКЛЮЧИТЕ обратно:
ALTER TABLE onboarding_templates ENABLE ROW LEVEL SECURITY;
ALTER TABLE onboarding_instances ENABLE ROW LEVEL SECURITY;
ALTER TABLE onboarding_tasks ENABLE ROW LEVEL SECURITY;
```

---

## 📝 Дополнительные файлы

- `check_onboarding_tables.sql` - полная проверка всех таблиц, политик и индексов
- `quick_check_onboarding.sql` - быстрая проверка основных параметров
- `refresh_schema_cache.sql` - обновление кеша схемы
- `TROUBLESHOOT_ONBOARDING_TABLES.md` - подробная инструкция по устранению проблем

---

## ⚡ Быстрое решение (если ничего не помогает)

1. Выполните `create_onboarding_tables.sql` еще раз
2. Выполните `refresh_schema_cache.sql`
3. Подождите 15 секунд
4. Очистите кеш браузера (Ctrl+Shift+R)
5. Обновите страницу (F5)


