# 🔍 Диагностика ошибки "Ошибка подключения к базе данных"

## Шаг 1: Проверьте консоль браузера

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Console**
3. Найдите сообщения, начинающиеся с `⚠️ Supabase не настроен`
4. Проверьте, какие значения показываются

## Шаг 2: Проверьте файл .env.local

### Убедитесь, что файл существует:

```bash
# В терминале в корне проекта:
ls -la .env.local
```

Должен показать файл с правами доступа.

### Проверьте содержимое файла:

Файл должен быть в корне проекта: `/Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2/.env.local`

Содержимое должно быть:
```env
VITE_SUPABASE_URL=https://supabase.assisthelp.ru
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlzcyI6InN1cGFiYXNlIiwiaWF0IjoxNzY1NjU5NjAwLCJleHAiOjE5MjM0MjYwMDB9.-DxQZP7k4N9kt6Xfe7gFjTVtiLZ7BIMJQz44PWFkz-k
```

**Важно:**
- Нет пробелов вокруг `=`
- Нет кавычек вокруг значений
- Каждая переменная на отдельной строке
- Нет пустых строк в начале файла

## Шаг 3: Перезапустите dev сервер

**КРИТИЧЕСКИ ВАЖНО:** Vite загружает `.env.local` только при старте!

1. **Остановите** сервер (Ctrl+C)
2. **Запустите** снова: `npm run dev`
3. **Обновите** страницу в браузере (F5)

## Шаг 4: Проверьте переменные окружения

В консоли браузера (F12 → Console) выполните:

```javascript
console.log('URL:', import.meta.env.VITE_SUPABASE_URL);
console.log('KEY:', import.meta.env.VITE_SUPABASE_ANON_KEY ? 'Есть (длина: ' + import.meta.env.VITE_SUPABASE_ANON_KEY.length + ')' : 'НЕТ');
```

Должно показать:
- URL: `https://supabase.assisthelp.ru`
- KEY: `Есть (длина: 200+)`

## Шаг 5: Проверьте настройки Supabase

1. Откройте Supabase Dashboard
2. Перейдите в **Project Settings** → **API**
3. Убедитесь, что:
   - **Project URL** совпадает с `VITE_SUPABASE_URL`
   - **anon public key** совпадает с `VITE_SUPABASE_ANON_KEY`

## Шаг 6: Альтернативное решение

Если ничего не помогает, используйте оффлайн-режим:

1. Попробуйте войти с любыми данными
2. Когда появится ошибка "Нет соединения с сервером"
3. Нажмите кнопку **"Войти в оффлайн-режиме"**

---

## 🐛 Частые проблемы:

### Проблема: "hasUrl: false"
**Решение:** Файл `.env.local` не найден или переменная `VITE_SUPABASE_URL` не загружена
- Проверьте, что файл в корне проекта
- Перезапустите сервер

### Проблема: "keyLength: 0"
**Решение:** Ключ не загружен
- Проверьте, что в `.env.local` есть `VITE_SUPABASE_ANON_KEY=...`
- Убедитесь, что нет пробелов вокруг `=`

### Проблема: "urlLength: 0"
**Решение:** URL не загружен
- Проверьте, что в `.env.local` есть `VITE_SUPABASE_URL=...`
- Убедитесь, что URL начинается с `https://`

### Проблема: Сервер не перезапущен
**Решение:** Vite не загрузит `.env.local` без перезапуска
- Остановите сервер (Ctrl+C)
- Запустите снова (`npm run dev`)

---

## ✅ Быстрая проверка:

Выполните в терминале (в корне проекта):

```bash
# Проверить файл
cat .env.local

# Проверить, что сервер запущен
ps aux | grep "vite\|node" | grep -v grep
```

Если файл пустой или не существует, создайте его заново (см. `FIX_CONNECTION.md`).

