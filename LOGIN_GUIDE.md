# 🔐 Руководство по входу в приложение

## ⚡ БЫСТРОЕ РЕШЕНИЕ (если не можете найти User Metadata)

**Если вы не можете найти раздел "User Metadata" в интерфейсе Supabase, используйте SQL Editor:**

**ВАЖНО:** Если получаете ошибку "column user_metadata does not exist", попробуйте варианты ниже:

### Вариант 1: Использовать raw_user_meta_data (для старых версий Supabase)

```sql
UPDATE auth.users
SET raw_user_meta_data = jsonb_set(
  COALESCE(raw_user_meta_data, '{}'::jsonb),
  '{is_admin}',
  'true'
)
WHERE email = 'ваш-email@gmail.com';
```

### Вариант 2: Использовать raw_app_meta_data (рекомендуется)

```sql
UPDATE auth.users
SET raw_app_meta_data = jsonb_set(
  COALESCE(raw_app_meta_data, '{}'::jsonb),
  '{role}',
  '"admin"'
)
WHERE email = 'ваш-email@gmail.com';
```

### Вариант 3: Проверить структуру таблицы

Сначала выполните этот запрос, чтобы увидеть, какие колонки есть:

```sql
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_schema = 'auth' 
AND table_name = 'users'
AND column_name LIKE '%meta%';
```

Это покажет правильные названия колонок в вашей версии Supabase.

---

## Проблема: "Старый вход не работает"

Если старые учетные данные не работают, есть несколько способов решить проблему:

---

## ✅ Вариант 1: Создать нового пользователя в Supabase

**⚠️ ВАЖНО: Порядок действий!**
1. **СНАЧАЛА** создайте пользователя
2. **ПОТОМ** назначьте права администратора через SQL

---

### Шаг 1: Откройте Supabase Dashboard

1. Перейдите на https://supabase.assisthelp.ru (или ваш URL Supabase)
2. Войдите в свой проект

### Шаг 2: Создайте нового пользователя (СНАЧАЛА ЭТО!)

**Способ A: Через Authentication UI (рекомендуется)**

1. Перейдите в **Authentication** → **Users**
2. Нажмите **"Add user"** → **"Create new user"**
3. Заполните:
   - **Email**: ваш email (например, `hrtisland@gmail.com`)
   - **Password**: придумайте надежный пароль
   - **Auto Confirm User**: включите ✅ (чтобы не требовалось подтверждение email)
4. Нажмите **"Create user"**
5. **Убедитесь, что пользователь создан** - он должен появиться в списке Users

**Способ B: Через SQL Editor (альтернатива)**

```sql
-- Создать пользователя (Supabase автоматически создаст запись в auth.users)
-- Используйте функцию Supabase для создания пользователя через API или Dashboard
```

### Шаг 3: Назначьте права администратора (ПОТОМ ЭТО!)

После создания пользователя нужно дать ему права администратора.

**Способ A: Через интерфейс Supabase Dashboard**

1. В **Authentication** → **Users** найдите созданного пользователя
2. **Кликните на строку с пользователем** (или на email) - откроется панель справа
3. В открывшейся панели найдите раздел **"Raw App Meta Data"** или **"Raw User Meta Data"**
4. Если раздел пустой, нажмите **"Edit"** или **"Add"**
5. Добавьте JSON:

**Для User Metadata (Raw User Meta Data):**
```json
{
  "is_admin": true
}
```

**Или для App Metadata (Raw App Meta Data):**
```json
{
  "role": "admin"
}
```

6. Нажмите **"Save"** или **"Update"**

**Если не видите раздел Metadata в интерфейсе, используйте Способ B (SQL):**

### Шаг 3 (Альтернатива): Назначить права через SQL Editor

1. Перейдите в **SQL Editor** в Supabase Dashboard
2. Создайте новый запрос
3. Выполните один из SQL запросов ниже (замените `your-email@example.com` на ваш email):

**ВАЖНО:** Если получаете ошибку "column does not exist", используйте `raw_user_meta_data` или `raw_app_meta_data` вместо `user_metadata`/`app_metadata`

**Вариант 1: Через raw_user_meta_data**
```sql
UPDATE auth.users
SET raw_user_meta_data = jsonb_set(
  COALESCE(raw_user_meta_data, '{}'::jsonb),
  '{is_admin}',
  'true'
)
WHERE email = 'your-email@example.com';
```

**Вариант 2: Через raw_app_meta_data (рекомендуется)**
```sql
UPDATE auth.users
SET raw_app_meta_data = jsonb_set(
  COALESCE(raw_app_meta_data, '{}'::jsonb),
  '{role}',
  '"admin"'
)
WHERE email = 'your-email@example.com';
```

**Если и это не работает, проверьте структуру таблицы:**
```sql
SELECT column_name 
FROM information_schema.columns 
WHERE table_schema = 'auth' 
AND table_name = 'users'
AND column_name LIKE '%meta%';
```

4. Нажмите **"Run"** или **"Execute"**
5. Проверьте результат - должно быть сообщение об успешном обновлении

### Шаг 4: Войдите в приложение

1. Откройте приложение
2. Введите:
   - **Email**: тот, который вы создали
   - **Password**: пароль, который вы установили
3. Нажмите **"Войти"**

---

## ✅ Вариант 2: Использовать оффлайн-режим (для тестирования)

Если у вас нет доступа к Supabase или нужно быстро протестировать приложение:

1. Откройте приложение
2. Попробуйте войти с любыми данными
3. Если появится ошибка "Нет соединения с сервером"
4. Нажмите кнопку **"Войти в оффлайн-режиме"**
5. Вы получите доступ к приложению в демо-режиме

**⚠️ Важно:** В оффлайн-режиме:
- Данные не сохраняются в базе данных
- Доступны только демо-данные
- Изменения не синхронизируются

---

## ✅ Вариант 3: Сбросить пароль существующего пользователя

Если пользователь уже существует, но вы забыли пароль:

1. На странице входа нажмите **"Забыли пароль?"**
2. Введите ваш email
3. Нажмите **"Отправить ссылку"**
4. Проверьте почту и перейдите по ссылке для сброса пароля

---

## 🔧 Проверка настроек Supabase

### Убедитесь, что файл `.env.local` создан

Файл должен содержать:

```env
VITE_SUPABASE_URL=https://supabase.assisthelp.ru
VITE_SUPABASE_ANON_KEY=ваш_ключ_здесь
```

### Получить актуальные ключи:

1. Зайдите в Supabase Dashboard
2. **Project Settings** → **API**
3. Скопируйте:
   - **Project URL** → `VITE_SUPABASE_URL`
   - **anon public key** → `VITE_SUPABASE_ANON_KEY`

### После изменения `.env.local`:

1. **Остановите** dev сервер (если запущен)
2. **Запустите** снова: `npm run dev`
3. Приложение подхватит новые настройки

---

## 🆘 Если ничего не помогает

### Проверьте консоль браузера

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Console**
3. Посмотрите на ошибки:
   - Если видите "Supabase не настроен" → проверьте `.env.local`
   - Если видите ошибки сети → проверьте URL Supabase
   - Если видите "Invalid login credentials" → пользователь не существует или неверный пароль

### Проверьте базу данных

В Supabase SQL Editor выполните:

```sql
-- Проверить всех пользователей
SELECT email, created_at, email_confirmed_at 
FROM auth.users;

-- Проверить метаданные администратора
SELECT 
  email,
  app_metadata->>'role' as role,
  user_metadata->>'is_admin' as is_admin
FROM auth.users;
```

---

## 📝 Администраторские email (для оффлайн-режима)

В файле `constants.ts` указан email для оффлайн-режима:
- `hrtisland@gmail.com`

Этот email используется только в оффлайн-режиме и для разработки.

---

## ✅ Быстрый старт

**Правильный порядок действий:**

1. ✅ Создайте файл `.env.local` в корне проекта с настройками Supabase
2. ✅ **СНАЧАЛА:** Создайте пользователя через Supabase Dashboard (Authentication → Users → Add user)
3. ✅ **ПОТОМ:** Назначьте права администратора через SQL Editor (см. ниже - самый надежный способ)
4. ✅ Войдите в приложение

**Или используйте оффлайн-режим для быстрого тестирования!**

---

## 🚀 САМЫЙ ПРОСТОЙ СПОСОБ: Назначить админа через SQL

**⚠️ ВАЖНО: Сначала создайте пользователя (Шаг 1), потом выполняйте SQL (Шаг 2)!**

### Шаг 1: Создайте пользователя (если еще не создан)

1. **Authentication** → **Users** → **Add user** → **Create new user**
2. Заполните email и пароль
3. Включите **Auto Confirm User** ✅
4. Нажмите **Create user**

### Шаг 2: Назначьте права администратора через SQL

Если не можете найти раздел Metadata в интерфейсе, используйте SQL Editor:

1. Откройте Supabase Dashboard
2. Перейдите в **SQL Editor** (в левом меню)
3. Нажмите **"New query"**
4. Вставьте этот код (замените `hrtisland@gmail.com` на ваш email):

```sql
-- Назначить администратора через raw_user_meta_data
-- Если не работает, попробуйте raw_app_meta_data (см. ниже)

UPDATE auth.users
SET raw_user_meta_data = jsonb_set(
  COALESCE(raw_user_meta_data, '{}'::jsonb),
  '{is_admin}',
  'true'
)
WHERE email = 'hrtisland@gmail.com';

-- ИЛИ через raw_app_meta_data (рекомендуется):
UPDATE auth.users
SET raw_app_meta_data = jsonb_set(
  COALESCE(raw_app_meta_data, '{}'::jsonb),
  '{role}',
  '"admin"'
)
WHERE email = 'hrtisland@gmail.com';
```

5. Нажмите **"Run"** (или Ctrl+Enter)
6. Должно появиться сообщение: "Success. No rows returned"
7. Готово! Теперь можете войти в приложение

**Проверить, что права назначены:**
```sql
SELECT 
  email,
  raw_user_meta_data->>'is_admin' as is_admin,
  raw_app_meta_data->>'role' as role
FROM auth.users
WHERE email = 'hrtisland@gmail.com';
```

Должно вернуть `is_admin: true` или `role: admin`

