# 🔄 Как перезагрузить dev сервер

## Быстрый способ

### Шаг 1: Остановить сервер

В терминале, где запущен `npm run dev`:
- Нажмите **Ctrl+C** (или Cmd+C на Mac)
- Сервер остановится

### Шаг 2: Запустить снова

```bash
cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2
npm run dev
```

---

## Если не знаете, где запущен сервер

### Вариант 1: Найти и остановить процесс

```bash
# Найти процесс
ps aux | grep vite

# Остановить процесс (замените PID на номер процесса)
kill -9 PID
```

### Вариант 2: Остановить все Node процессы (осторожно!)

```bash
# Остановить все процессы node
pkill -f node

# Или только vite
pkill -f vite
```

### Вариант 3: Закрыть терминал

- Просто закройте терминал, где запущен сервер
- Откройте новый терминал
- Запустите `npm run dev` снова

---

## Полная перезагрузка (если ничего не помогает)

```bash
# 1. Перейти в проект
cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2

# 2. Остановить все процессы
pkill -f vite
pkill -f node

# 3. Подождать 2 секунды
sleep 2

# 4. Запустить сервер
npm run dev
```

---

## Проверка, что сервер запущен

После запуска `npm run dev` должно появиться:

```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

Если видите это сообщение - сервер работает!

---

## Если сервер не запускается

### Проблема: "Port 5173 is already in use"

```bash
# Найти процесс на порту 5173
lsof -ti:5173

# Остановить процесс
kill -9 $(lsof -ti:5173)

# Запустить снова
npm run dev
```

### Проблема: "vite: command not found"

```bash
# Установить зависимости
npm install

# Запустить снова
npm run dev
```

### Проблема: Ошибки при запуске

```bash
# Очистить кэш
npm cache clean --force

# Удалить node_modules и переустановить
rm -rf node_modules package-lock.json
npm install

# Запустить снова
npm run dev
```

---

## После перезапуска

1. **Обновите страницу в браузере** (F5 или Ctrl+R)
2. **Проверьте консоль браузера** (F12) на ошибки
3. **Проверьте, что данные загружаются**

---

## Быстрая команда для перезапуска

Создайте алиас в терминале (опционально):

```bash
# Добавить в ~/.zshrc или ~/.bashrc
alias restart-dev="cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2 && pkill -f vite && sleep 1 && npm run dev"
```

Тогда можно просто выполнить:
```bash
restart-dev
```

---

## ⚠️ Важно

После изменений в коде:
- **Обычно** Vite автоматически перезагружает страницу (Hot Module Replacement)
- **Иногда** нужно перезагрузить страницу вручную (F5)
- **Редко** нужно перезапустить сервер (Ctrl+C и `npm run dev`)

**Перезапуск сервера нужен только если:**
- Изменили `.env.local`
- Изменили `vite.config.ts`
- Изменили `package.json`
- Сервер завис или не отвечает





