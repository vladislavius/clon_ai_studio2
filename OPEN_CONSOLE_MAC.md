# 🖥️ Как открыть консоль разработчика на Mac

## 🔍 Быстрые способы

### Safari

**Способ 1: Горячие клавиши**
- `Cmd + Option + I` (⌘ + ⌥ + I)
- Или `Cmd + Option + C` (⌘ + ⌥ + C) - только консоль

**Способ 2: Через меню**
1. Safari → Настройки → Дополнения
2. Включите "Показывать меню "Разработка" в строке меню"
3. Разработка → Показать веб-инспектор

---

### Chrome / Edge

**Горячие клавиши:**
- `Cmd + Option + I` (⌘ + ⌥ + I) - полный DevTools
- `Cmd + Option + J` (⌘ + ⌥ + J) - только консоль
- `Cmd + Shift + C` (⌘ + ⇧ + C) - режим выбора элементов

**Через меню:**
1. Вид → Разработчик → Инструменты разработчика
2. Или правый клик на странице → "Просмотреть код"

---

### Firefox

**Горячие клавиши:**
- `Cmd + Option + I` (⌘ + ⌥ + I) - полный DevTools
- `Cmd + Option + K` (⌘ + ⌥ + K) - только консоль
- `F12` - может работать, если включено в настройках

**Через меню:**
1. Инструменты → Веб-разработка → Инструменты разработчика
2. Или правый клик → "Исследовать элемент"

---

## ⌨️ Почему F12 не работает на Mac?

На Mac клавиша **F12** по умолчанию используется для:
- Управления громкостью (F11, F12)
- Или других системных функций

### Как включить F12 для DevTools

**В Chrome/Edge:**
1. Системные настройки → Клавиатура
2. Включите "Использовать клавиши F1, F2 и т.д. как стандартные функциональные клавиши"
3. Или нажимайте `Fn + F12`

**В Safari:**
- F12 обычно не работает
- Используйте `Cmd + Option + I`

---

## 🎯 Рекомендуемые горячие клавиши для Mac

| Действие | Chrome/Edge | Safari | Firefox |
|----------|-------------|--------|---------|
| Открыть DevTools | `⌘ + ⌥ + I` | `⌘ + ⌥ + I` | `⌘ + ⌥ + I` |
| Только консоль | `⌘ + ⌥ + J` | `⌘ + ⌥ + C` | `⌘ + ⌥ + K` |
| Выбор элементов | `⌘ + ⇧ + C` | `⌘ + ⇧ + C` | `⌘ + ⇧ + C` |
| Перезагрузить | `⌘ + R` | `⌘ + R` | `⌘ + R` |
| Жесткая перезагрузка | `⌘ + ⇧ + R` | `⌘ + ⇧ + R` | `⌘ + ⇧ + R` |

---

## 🔧 Настройка Safari для разработки

Safari по умолчанию скрывает меню разработчика:

1. **Включить меню разработчика:**
   - Safari → Настройки → Дополнения
   - Включите "Показывать меню "Разработка" в строке меню"

2. **Открыть консоль:**
   - Разработка → Показать веб-инспектор
   - Или `Cmd + Option + I`

---

## 📱 Альтернативные способы

### Через правый клик (контекстное меню)

1. **Правый клик** на любом элементе страницы
2. Выберите:
   - Chrome/Edge: "Просмотреть код" или "Проверить"
   - Safari: "Проверить элемент"
   - Firefox: "Исследовать элемент"

### Через адресную строку

В Chrome/Edge можно ввести в адресной строке:
```
chrome://inspect
```
Или просто нажать `Cmd + Option + I`

---

## 🎓 Полезные команды в консоли

После открытия консоли можно выполнить:

```javascript
// Проверить переменные окружения
console.log('URL:', import.meta.env.VITE_SUPABASE_URL);
console.log('KEY:', import.meta.env.VITE_SUPABASE_ANON_KEY ? 'Есть' : 'НЕТ');

// Проверить данные департамента
console.log('Текущий департамент:', window.currentDept);

// Очистить консоль
console.clear();
```

---

## ⚡ Быстрая справка

**Самый быстрый способ на Mac:**
- Chrome/Edge: `Cmd + Option + J` (только консоль)
- Safari: `Cmd + Option + C` (только консоль)
- Firefox: `Cmd + Option + K` (только консоль)

**Для полного DevTools:**
- Все браузеры: `Cmd + Option + I`

---

## 🆘 Если ничего не работает

1. **Проверьте настройки клавиатуры:**
   - Системные настройки → Клавиатура
   - Убедитесь, что функциональные клавиши настроены правильно

2. **Используйте меню браузера:**
   - Вид → Разработчик → Инструменты разработчика

3. **Правый клик:**
   - Правый клик на странице → "Проверить элемент"

---

**Самый простой способ:** `Cmd + Option + J` в Chrome/Edge или `Cmd + Option + C` в Safari! 🚀





