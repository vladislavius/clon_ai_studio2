# 💻 Команды терминала для работы с проектом

## 📂 Переход в директорию проекта

### Способ 1: Абсолютный путь (всегда работает)

```bash
cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2
```

### Способ 2: Относительный путь (если вы в Desktop)

```bash
cd Desktop/clone_ai_studio2/clon_ai_studio2
```

### Способ 3: Если вы уже в папке clone_ai_studio2

```bash
cd clon_ai_studio2
```

## ✅ Проверка, что вы в правильной директории

После перехода выполните:

```bash
pwd
```

Должно показать:
```
/Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2
```

## 📋 Полезные команды для проверки

### Проверить файл .env.local

```bash
# Проверить, что файл существует
ls -la .env.local

# Посмотреть содержимое файла
cat .env.local

# Или открыть в редакторе
open .env.local
```

### Проверить структуру проекта

```bash
# Показать все файлы (включая скрытые)
ls -la

# Показать только видимые файлы
ls
```

### Запустить dev сервер

```bash
# Установить зависимости (если еще не установлены)
npm install

# Запустить dev сервер
npm run dev
```

### Проверить статус Git

```bash
# Показать статус репозитория
git status

# Показать последние коммиты
git log --oneline -5
```

## 🔗 Работа с GitHub (не в терминале!)

**Важно:** GitHub - это веб-сайт, туда нельзя "перейти" в терминале.

Но можно работать с удаленным репозиторием:

### Посмотреть URL репозитория

```bash
git remote -v
```

Покажет:
```
origin  https://github.com/vladislavius/clon_ai_studio2.git (fetch)
origin  https://github.com/vladislavius/clon_ai_studio2.git (push)
```

### Открыть репозиторий в браузере

```bash
# macOS
open https://github.com/vladislavius/clon_ai_studio2

# Или просто скопируйте URL в браузер:
# https://github.com/vladislavius/clon_ai_studio2
```

### Отправить изменения на GitHub

```bash
# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push origin main
```

## 📝 Быстрая справка

| Действие | Команда |
|----------|---------|
| Перейти в проект | `cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2` |
| Проверить текущую директорию | `pwd` |
| Проверить .env.local | `ls -la .env.local` |
| Посмотреть содержимое .env.local | `cat .env.local` |
| Запустить сервер | `npm run dev` |
| Остановить сервер | `Ctrl+C` |
| Открыть GitHub в браузере | `open https://github.com/vladislavius/clon_ai_studio2` |

## 🎯 Пример полного цикла

```bash
# 1. Перейти в проект
cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2

# 2. Проверить, что вы в правильной директории
pwd

# 3. Проверить файл .env.local
ls -la .env.local

# 4. Посмотреть содержимое
cat .env.local

# 5. Запустить сервер
npm run dev
```




