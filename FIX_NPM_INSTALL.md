# 🔧 Исправление ошибки "vite: command not found"

## Проблема

Ошибка `vite: command not found` означает, что зависимости проекта не установлены.

## ✅ Решение

### Шаг 1: Установите зависимости

В терминале выполните:

```bash
cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2
npm install
```

Это установит все зависимости, включая `vite`.

### Шаг 2: Если возникает ошибка прав доступа

Если видите ошибку `EPERM: operation not permitted`, попробуйте:

#### Вариант 1: Использовать локальную установку (рекомендуется)

```bash
# Убедитесь, что вы в папке проекта
cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2

# Очистите кэш npm
npm cache clean --force

# Установите зависимости
npm install
```

#### Вариант 2: Использовать npx (если vite установлен глобально)

```bash
cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2
npx vite
```

#### Вариант 3: Проверить права доступа

```bash
# Проверить права на папку проекта
ls -la /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2

# Если нужно, исправить права
chmod -R 755 /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2
```

### Шаг 3: После установки запустите сервер

```bash
npm run dev
```

Должно показать что-то вроде:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

---

## 🔍 Проверка установки

### Проверить, что зависимости установлены:

```bash
# Проверить наличие node_modules
ls -la node_modules | head -5

# Проверить, что vite установлен
ls node_modules/.bin/vite
```

### Проверить версию Node.js и npm:

```bash
node --version
npm --version
```

Рекомендуется:
- Node.js: v18 или выше
- npm: v9 или выше

---

## 🆘 Если ничего не помогает

### 1. Удалите node_modules и package-lock.json:

```bash
cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2
rm -rf node_modules package-lock.json
npm install
```

### 2. Используйте yarn (альтернатива npm):

```bash
# Установите yarn (если еще не установлен)
npm install -g yarn

# Установите зависимости через yarn
yarn install

# Запустите сервер
yarn dev
```

### 3. Проверьте, что вы в правильной директории:

```bash
# Должно показать путь к проекту
pwd

# Должно показать package.json
ls package.json
```

---

## ✅ После успешной установки

1. **Запустите сервер:**
   ```bash
   npm run dev
   ```

2. **Откройте браузер:**
   - Обычно это `http://localhost:5173`
   - Точный адрес покажет Vite в терминале

3. **Проверьте подключение:**
   - Откройте консоль браузера (F12)
   - Проверьте, нет ли ошибок подключения к Supabase

---

## 📝 Быстрая справка

| Проблема | Решение |
|----------|---------|
| `vite: command not found` | `npm install` |
| `EPERM: operation not permitted` | `npm cache clean --force` затем `npm install` |
| Зависимости не устанавливаются | Удалите `node_modules` и `package-lock.json`, затем `npm install` |
| Старая версия Node.js | Обновите Node.js до v18+ |

---

## 🎯 Полная последовательность команд

```bash
# 1. Перейти в проект
cd /Users/vtsv/Desktop/clone_ai_studio2/clon_ai_studio2

# 2. Проверить, что package.json существует
ls package.json

# 3. Установить зависимости
npm install

# 4. Запустить сервер
npm run dev
```






