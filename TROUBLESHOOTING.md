# 🔧 Устранение неполадок

## Исправленные проблемы

### 1. ✅ WebSocket ошибка
- **Исправлено:** Добавлена явная конфигурация HMR в `vite.config.ts`
- **Действие:** Перезапустите dev сервер: `npm run dev`

### 2. ✅ Избыточное логирование
- **Исправлено:** Заменены все `console.log` на функции из `utils/logger.ts`
- **Результат:** Логирование работает только в dev режиме

### 3. ✅ Оптимизация загрузки статистик
- **Исправлено:** Добавлена сортировка на стороне БД
- **Действие:** Выполните SQL-скрипт `optimize_statistics_performance.sql` для добавления индексов

### 4. ✅ Autocomplete для пароля
- **Исправлено:** Добавлен `autoComplete="current-password"` в поле пароля

---

## Проблема: Сотрудники не загружаются (показывает 0)

### Возможные причины:

1. **Проблема с RLS (Row Level Security)**
   - Проверьте политики доступа в Supabase
   - Убедитесь, что текущий пользователь имеет права на чтение таблицы `employees`

2. **Таблица пуста**
   - Проверьте в Supabase Dashboard, есть ли данные в таблице `employees`

3. **Ошибка подключения**
   - Проверьте консоль браузера на наличие ошибок
   - Убедитесь, что переменные окружения настроены правильно

### Диагностика:

1. **Откройте консоль браузера (F12)**
   - Проверьте наличие ошибок при загрузке
   - Ищите сообщения с префиксом `[useEmployees]`

2. **Проверьте в Supabase Dashboard:**
   ```sql
   SELECT COUNT(*) FROM employees;
   ```

3. **Проверьте RLS политики:**
   ```sql
   SELECT * FROM pg_policies WHERE tablename = 'employees';
   ```

4. **Проверьте переменные окружения:**
   - Убедитесь, что `.env.local` содержит правильные значения
   - `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`

### Быстрое решение:

Если сотрудники не загружаются, попробуйте:

1. **Очистить кэш и перезагрузить:**
   ```javascript
   // В консоли браузера
   localStorage.clear();
   location.reload();
   ```

2. **Проверить подключение к Supabase:**
   - Откройте Network tab в DevTools
   - Проверьте запросы к Supabase API
   - Убедитесь, что они возвращают статус 200

3. **Проверить права доступа:**
   - В Supabase Dashboard → Authentication → Policies
   - Убедитесь, что есть политика для чтения `employees`

---

## Проблема: Статистики загружаются медленно (15 секунд)

### Решение:

1. **Выполните SQL-скрипт для оптимизации:**
   ```sql
   -- Запустите optimize_statistics_performance.sql в Supabase SQL Editor
   ```

2. **Проверьте количество записей:**
   ```sql
   SELECT COUNT(*) FROM statistics_values;
   ```
   - Если записей очень много (>10000), рассмотрите пагинацию

3. **Проверьте индексы:**
   ```sql
   SELECT indexname, indexdef 
   FROM pg_indexes 
   WHERE tablename = 'statistics_values';
   ```

### Оптимизация запроса:

Запрос уже оптимизирован:
- Используется только необходимые колонки
- Сортировка выполняется на стороне БД
- Индексы ускоряют выполнение

---

## Следующие шаги

1. ✅ Перезапустите dev сервер
2. ✅ Выполните SQL-скрипт для оптимизации БД
3. ⏳ Проверьте RLS политики для таблицы `employees`
4. ⏳ Проверьте наличие данных в таблице `employees`

---

## Логи для отладки

В dev режиме логи показывают:
- `[useEmployees]` - загрузка сотрудников
- `[useOrgStructure]` - загрузка структуры организации
- `[StatisticsTab]` - загрузка статистик

Проверьте консоль браузера для диагностики проблем.



