# 🔧 Исправление ошибки React Hooks

## Проблема

Ошибка: `Invalid hook call. Hooks can only be called inside of the body of a function component`

Это происходит из-за дублирования React в приложении.

## Решение

### 1. Обновлен `vite.config.ts`

Добавлены настройки для устранения дублирования React:
- `resolve.alias` - принудительное использование одной копии React
- `resolve.dedupe` - удаление дубликатов
- `optimizeDeps.include` - предварительная оптимизация React

### 2. Что нужно сделать:

1. **Остановите dev сервер** (Ctrl+C)

2. **Очистите кэш Vite:**
   ```bash
   rm -rf node_modules/.vite
   ```

3. **Переустановите зависимости (опционально, если проблема сохраняется):**
   ```bash
   rm -rf node_modules
   npm install
   ```

4. **Перезапустите dev сервер:**
   ```bash
   npm run dev
   ```

### 3. Если проблема сохраняется:

Проверьте, нет ли нескольких версий React:
```bash
npm list react react-dom
```

Должна быть только одна версия каждого пакета.

### 4. Альтернативное решение:

Если проблема все еще есть, попробуйте:
```bash
npm install react@^18.2.0 react-dom@^18.2.0 --save-exact
```

---

## Исправления WebSocket

Также исправлена конфигурация WebSocket:
- Используется `127.0.0.1` вместо `localhost` для более надежного подключения
- Явно указан `protocol: 'ws'` и `clientPort`

После перезапуска сервера WebSocket должен работать корректно.


