# 🚀 Настройка переменных окружения в Vercel

## Проблема
Приложение работает на localhost, но на Vercel не подключается к Supabase, потому что переменные окружения не настроены.

## Решение: Добавить переменные окружения в Vercel

### Шаг 1: Получите ключи Supabase

1. Зайдите в ваш проект Supabase
2. Перейдите в **Project Settings** → **API**
3. Скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public key** (длинная строка, начинается с `eyJ...`)

### Шаг 2: Добавьте переменные в Vercel

1. Зайдите на [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект `clon_ai_studio2`
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте следующие переменные:

#### Для Production, Preview и Development:

**Переменная 1:**
- **Name:** `VITE_SUPABASE_URL`
- **Value:** `https://your-project.supabase.co` (ваш Project URL)
- **Environment:** Выберите все (Production, Preview, Development)

**Переменная 2:**
- **Name:** `VITE_SUPABASE_ANON_KEY`
- **Value:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...` (ваш anon public key)
- **Environment:** Выберите все (Production, Preview, Development)

### Шаг 3: Пересоберите проект

После добавления переменных окружения:

1. В Vercel Dashboard перейдите в **Deployments**
2. Найдите последний deployment
3. Нажмите на **"..."** (три точки) → **Redeploy**
4. Или просто сделайте новый commit и push - Vercel автоматически пересоберет проект

### Шаг 4: Проверьте

После пересборки:
1. Откройте ваш сайт на Vercel
2. Откройте консоль браузера (F12)
3. Должно исчезнуть предупреждение: `⚠️ Supabase не настроен`
4. Приложение должно подключиться к базе данных

---

## ⚠️ Важно

- **НЕ добавляйте** переменные окружения в код или в git
- Файл `.env.local` используется только для локальной разработки
- В Vercel переменные окружения настраиваются через Dashboard
- После добавления переменных **обязательно пересоберите** проект

---

## 🔍 Проверка текущих значений

Если хотите проверить, какие переменные окружения используются на Vercel:

1. В Vercel Dashboard → **Settings** → **Environment Variables**
2. Убедитесь, что обе переменные присутствуют:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`

---

## 📝 Пример правильных значений

```env
VITE_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlzcyI6InN1cGFiYXNlIiwiaWF0IjoxNzY1NjU5NjAwLCJleHAiOjE5MjM0MjYwMDB9.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

**Важно:**
- URL должен начинаться с `https://`
- ANON_KEY должен быть длиннее 20 символов (обычно ~200+ символов)
- Не должно быть пробелов или переносов строк

---

## 🆘 Если не работает

1. **Проверьте формат:**
   - URL начинается с `https://`
   - Ключ не содержит пробелов
   - Обе переменные добавлены для всех окружений (Production, Preview, Development)

2. **Пересоберите проект:**
   - В Vercel Dashboard → Deployments → Redeploy последний deployment

3. **Проверьте логи:**
   - В Vercel Dashboard → Deployments → выберите deployment → Functions/Logs
   - Ищите ошибки связанные с переменными окружения

4. **Очистите кэш:**
   - В Vercel Dashboard → Settings → General → Clear Build Cache
   - Затем сделайте Redeploy

---

## ✅ После настройки

После правильной настройки переменных окружения:
- ✅ Приложение подключится к Supabase
- ✅ Будет работать аутентификация
- ✅ Будут загружаться данные из базы
- ✅ Исчезнет предупреждение в консоли




